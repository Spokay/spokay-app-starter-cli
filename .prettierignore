node_modules/
dist/
dist-test/
coverage/
package-lock.json

# agent tooling, not shipped source
.claude/

# `__TOKEN__` is also markdown emphasis syntax, so prettier rewrites a bare occurrence
# into `\***\*TOKEN\*\***`. The docs here are full of token names; one outside a code span
# would be silently corrupted.
*.md
